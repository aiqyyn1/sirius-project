const secretAccess = {
      secret: 'KEY_ACCESS',
};
const secretRefresh = {
      secret: 'KEY_REFRESH',      
};
const secretReset = {
      secret: 'KEY_RESET',
}
module.exports = {secretAccess, secretRefresh, secretReset}
